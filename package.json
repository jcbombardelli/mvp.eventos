{
  "name": "spass",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "install-all": "cd server && npm install && cd .. && cd client && npm install",
    "start": "cd server && npm install && cd .. && npm start --prefix server/",
    "start-complete": "npm run build --prefix client/ && npm start --prefix server/",
    "prod": "cd server && npm install && cd .. && cd client && npm install && cd .. && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "JC Bombardelli",
  "license": "MIT"
}